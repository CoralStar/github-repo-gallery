const overview = document.querySelector(".overview");
const unsername = "CoralStar";

const gitUserInfo = async function () {
    const userInfo = await fetch(`https://api.github.com/users/${username}`);
};
